# generator.py


usage: generator [-h] [-v [VERBOSE]]

Generate SQL, Protobuf, and Python data structures

options:
  -h, --help            show this help message and exit
  -v [VERBOSE], --verbose [VERBOSE]
                        verbose output

        This should only be run standalone for debugging purposes.
